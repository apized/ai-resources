# Apized AI Resources

Claude Code marketplace containing the Apized framework skill.

## Install

In Claude Code, add this repository as a marketplace and install the plugin:

```text
/plugin marketplace add apized/ai-resources
/plugin install apized@apized-tools
```

The skill is then available as `/apized:apized` and can also activate automatically for Apized framework questions.
